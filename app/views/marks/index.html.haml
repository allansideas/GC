#mobile
  #marks
    #timers
    #show_time.button Show time
    #timer_buttons
      #start.button Start
      #split.button Split
      #end.button End
    #marks_list
  :javascript
    console.log(#{@marks.to_json});
    window.controller = new App.Controllers.MarksController({marks: #{@marks.to_json}});                                                                                                                   
    Backbone.history.start();

